<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0/EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.morph.mapper.RoleMapper">

    <!---->
    <select id="detail" parameterType="hashMap" resultType="org.example.morph.dto.RoleDto$DetailResDto">
        SELECT role.role_name as roleName
             , role.content
        FROM role
        WHERE role.id = #{id}
    </select>

    <select id="list" parameterType="hashMap" resultType="org.example.morph.dto.RoleDto$ListResDto">
        SELECT role.id
             , role.role_name as roleName
             , role.user_id as userId
             , role.created_at as createdAt
             , role.modified_at as modifiedAt
             , role.deleted
        FROM role
        WHERE role.id is not NULL
        <if test = "deleted != null">AND role.deleted =  #{deleted}</if>
    </select>

</mapper>