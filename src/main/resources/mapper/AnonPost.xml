<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0/EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.morph.mapper.AnonPostMapper">

    <!--Detail-->
    <select id="detail" parameterType="hashMap" resultType="org.example.morph.dto.AnonPostDto$DetailResDto">
        SELECT anon_post.id,
               anon_post.deleted,
               anon_post.created_at as createdAt,
               anon_post.modified_at as modifiedAt,
               anon_post.content
        FROM anon_post
        WHERE anon_post.id = #{id}
    </select>

    <!--List-->
    <select id="list" parameterType="hashMap" resultType="org.example.morph.dto.AnonPostDto$ListResDto">
        SELECT anon_post.id,
               anon_post.modified_at as modifiedAt,
               anon_post.content
        FROM anon_post
        WHERE anon_post.id is not NULL
    </select>

</mapper>