<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0/EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.morph.mapper.OffPostMapper">

    <!--Detail-->
    <select id="detail" parameterType="hashMap" resultType="org.example.morph.dto.OffPostDto$DetailResDto">
        SELECT offPost.id,
               offPost.deleted,
               offPost.create_user_id as createUserId,
               offPost.content,
               offPost.created_at as createdAt,
               offPost.modified_at as modifiedAt
        FROM offPost
        WHERE offPost.id = #{id}
    </select>

    <!--List-->
    <select id="list" parameterType="hashMap" resultType="org.example.morph.dto.OffPostDto$ListResDto">
        SELECT offPost.id,
               offPost.content,
               offPost.created_at as createdAt,
               offPost.modified_at as modifiedAt
        FROM offPost
        WHERE offPost.id is not NULL
          AND offPost.deleted = 0
    </select>

</mapper>